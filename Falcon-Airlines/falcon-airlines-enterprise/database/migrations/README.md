# Database Migrations

This folder is a reference copy of the approved database design.

Active migrations live in `backend/src/main/resources/db/migration/` and are executed by Flyway.

See `DATABASE_DESIGN.md` in the parent project for the full schema.
